import { Fragment } from 'react';
import Navbar from './../layout/navbar/Nabar';

const Catalogue = () => {
    return (
        <Fragment>
            <Navbar/>
        </Fragment>
    );
}

export default Catalogue;